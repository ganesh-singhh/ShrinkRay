services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=mongodb://your_mongodb_uri
      - REDIS_ADDR=127.0.0.1:6379
      - REDIS_PASSWORD=your_redis_password
      - REDIS_DB=0
      - GIN_MODE=release